import type { ParameterMapping } from '../types';

/** Nombres exactos de parámetros enviados al webhook de n8n. */
export const OFFICIAL_PARAMETER_MAPPING: ParameterMapping = {
  nombre: 'Nombre',
  email: 'Email',
  empresa: 'Empresa',
  pais: 'Pais',
  ciudad: 'Ciudad',
  mensaje: 'Mensaje',
};
